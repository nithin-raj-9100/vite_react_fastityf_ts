{
  "version": 2,
  "buildCommand": "pnpm run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ],
  "functions": {
    "api/**/*": {
      "runtime": "nodejs18.x"
    }
  }
}
