{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "vercel-build": "turbo run build && mkdir -p dist && cp -r apps/web/dist/* dist/ && mkdir -p dist/api && cp api/[[...route]].js dist/api/ && mkdir -p dist/apps/backend/dist && cp -r apps/backend/dist/* dist/apps/backend/dist/",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.4.4"
  },
  "packageManager": "pnpm@8.15.6",
  "name": "with-vite-react"
}
